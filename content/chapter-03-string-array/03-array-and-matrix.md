---
title: "3.3 数组寻址与特殊矩阵"
description: "多维数组的一维存储寻址，以及对称、三角、稀疏矩阵的压缩方案。"
order: 3
chapter: 3
chapterTitle: "字符串与数组"
updated: "2026-08-12"
contributors: ["Azen"]
status: "draft"
---

# 3.3 数组寻址与特殊矩阵

数组本身是"按下标随机存取"的数据结构：逻辑上它可能有多维，物理上它占据一段连续内存。理解**多维下标到一维地址的映射**，是掌握数组存储的关键；理解**特殊矩阵的压缩**，则是空间优化的典型练习。

## 学习目标

- 由二维数组的行优先存储推导寻址公式；
- 用压缩存储表示对称矩阵与三角矩阵，推导下标换算；
- 用三元组表示稀疏矩阵，分析空间收益。

## 行优先寻址

C 语言采用**行优先**存储：先存第 0 行全部元素，再存第 1 行……设二维数组 `A[m][n]` 的首地址为 `base`，每个元素占 `size` 字节，则元素 `A[i][j]` 的地址为：

$$
\mathrm{addr}(A[i][j]) = base + (i \times n + j) \times size.
$$

```cpp:line-numbers [row-major.cpp]
// 下标 (i, j) 在行优先一维数组中的偏移
std::size_t row_major_offset(std::size_t i, std::size_t j, std::size_t cols) {
    return i * cols + j;
}
```

$$O(1)$$ 的随机访问正是由这一固定公式保证的。列优先（如 Fortran）把公式换成 `j * rows + i`，两种约定结果不同，跨语言传数组时必须在文档中写清。

## 对称矩阵压缩

若 `A` 是 $n \times n$ 对称矩阵（`A[i][j] = A[j][i]`），只需存储下三角（含对角线）共：

$$
\frac{n(n+1)}{2}
$$

个元素，用一维数组 `B` 保存。访问 `A[i][j]`（$i \ge j$）时按下三角行优先换算：

$$
k = \frac{i(i+1)}{2} + j.
$$

当 `i < j` 时利用对称性交换下标即可。

## 三角矩阵与对角矩阵

上三角矩阵只需存储主对角线及其上方：`A[i][j]`（$i \le j$）映射为

$$
k = \frac{i(2n - i + 1)}{2} + (j - i).
$$

对角矩阵只在主对角线有非零元，只需一个长度为 `n` 的数组，`A[i][i]` 存 `B[i]`。三者空间都从 $O(n^2)$ 降到 $O(n^2/2)$ 或 `O(n)`。

## 稀疏矩阵与三元组

当非零元个数 `t` 远小于 $n^2$（如 $t < n^2/5$），直接存二维数组会浪费大量空间。**三元组表**只记录每个非零元的三项：`(row, col, value)`，并按行优先排序。

```cpp:line-numbers [sparse-triple.cpp]
struct Triple { std::size_t row, col; int value; };

// 稀疏矩阵：行数、列数、非零元个数 + 三元组表
struct SparseMatrix {
    std::size_t rows, cols;
    std::vector<Triple> data;   // 按 (row, col) 有序
};
```

空间代价从 $O(n^2)$ 降到 `O(t)`。代价是随机访问 `A[i][j]` 需要查找三元组，无法直接寻址——若按行存储可在行内二分，最坏 `O(log t)`；转置等操作则需重排三元组顺序（见配套 Lab 03-02）。

::: warning 压缩是有代价的
压缩把空间换成时间：对称矩阵的下标换算保持 `O(1)` 访问，而稀疏矩阵三元组的访问依赖查找。不要只看空间收益，操作模式（随机访问 vs 批量处理）决定哪种方案合适。
:::

## 小结

数组的寻址公式是"连续内存 + 固定步长"的直接产物；特殊矩阵的压缩利用的是矩阵的结构规律。判断能否压缩、如何换算下标，比死记公式更重要——遇到新类型的结构矩阵时，方法是一样的。

## 练习

1. 推导列优先存储下 `A[i][j]` 的地址公式。
2. 对称矩阵压缩存储后，访问 `A[j][i]`（`j < i`）时下标如何换算？
3. 稀疏度 $t/n^2$ 达到多少时，三元组表才比二维数组省空间（按字节估算）？
4. 为三元组表的转置设计一个 `O(t + n)` 的算法思路（提示：先统计每行非零元个数）。
