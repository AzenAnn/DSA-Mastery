# 第 4 章树与二叉树理论题整理

## Goal

把用户提供的“树与二叉树”题库中 `01`～`08` 八份 Markdown 文档整理为 DSA Mastery 第 4 章可浏览、可交互、可复核的八个理论题 Lab，并把入口写入第 4 章内容目录。读者既能在网页完成四选一练习，也能在同一主题下继续完成综合题。

## Background

- 当前开发者为 `Azen`，工作分支为 `codex/chapter4-theory-exercises`，已快进到远端最新 `origin/chapter/04`（`b73ecb4`）；最终 PR 目标分支为 `chapter/04`，不使用旧的 `chapter4`。
- 题目只来自用户指定目录的 `01`～`08` 文件；`_目录.md`、`09`～`13` 和 `98-跨章综合.md` 不读取、不导入。
- 已阅读 `docs/UPDATE_WORKFLOW.md` 与 `docs/LAB_AUTHORING_GUIDE.md`。现有 Lab Schema v1 的 Quiz 只支持四选一，题目唯一事实来源是 `quiz.json`，README 只挂载一次 `<QuizSet />`。
- 最新 `chapter/04` 已有 14 个有效 Program Lab（order 1～14），并启用了第 4 章 Lab 自动分类展示；新理论题必须顺延为 order 15～22，不手写侧栏。
- 八份源文件实际题量与本次入库题量如下：

| 源文件 | 原选择题 | 入库选择题 | 原综合题 | 入库综合题 |
| --- | ---: | ---: | ---: | ---: |
| 01 二叉树基础（性质+存储） | 38 | 20 | 7 | 5 |
| 02 前序遍历 | 12 | 12 | 1 | 1 |
| 03 中序遍历 | 11 | 11 | 3 | 3 |
| 04 后序遍历 | 14 | 14 | 0 | 0 |
| 05 层序遍历 | 8 | 8 | 0 | 0 |
| 06 由遍历序列构造二叉树 | 18 | 18 | 2 | 2 |
| 07 线索二叉树 | 14 | 14 | 0 | 0 |
| 08 树与森林 | 22 | 20 | 6 | 5 |
| **合计** | **137** | **117** | **19** | **16** |

## Requirements

### R1. 严格限定来源与摘取规则

- 每份文档按原有题目顺序摘取：选择题取前 `min(20, 实际数量)` 道，综合题取前 `min(5, 实际数量)` 道。
- 不跨文件补足数量，不从 `09` 及之后的文档补题，不新增或改编题目。
- 保留题面、选项、答案、解析、难度和考点；按用户验收反馈移除页面中的来源、来源链接与题目标识。
- 已机械确认本次 117 道选择题都恰好有四个选项、答案和内部唯一 id，能够进入现有 Quiz Schema。

### R2. 建立八个第 4 章理论题 Lab

按最新远端的 14 个 Program Lab 顺延创建：

| order | Lab 目录 | 主题 |
| ---: | --- | --- |
| 15 | `lab-04-15-binary-tree-basics-quiz` | 二叉树基础（性质与存储） |
| 16 | `lab-04-16-preorder-traversal-quiz` | 前序遍历 |
| 17 | `lab-04-17-inorder-traversal-quiz` | 中序遍历 |
| 18 | `lab-04-18-postorder-traversal-quiz` | 后序遍历 |
| 19 | `lab-04-19-level-order-traversal-quiz` | 层序遍历 |
| 20 | `lab-04-20-reconstruct-binary-tree-quiz` | 由遍历序列构造二叉树 |
| 21 | `lab-04-21-threaded-binary-tree-quiz` | 线索二叉树 |
| 22 | `lab-04-22-trees-and-forests-quiz` | 树与森林 |

- 每个目录包含合法 frontmatter 的 `README.md`、Schema v1 的 `lab.json` 和选择题唯一数据源 `quiz.json`。
- 每份 README 包含目标、前置知识、题量说明、作答方法、唯一 `<QuizSet />`、综合题、完成清单与复盘。
- 选择题只存在于 `quiz.json`；综合题及其折叠参考答案只存在于 README，不与 Quiz 数据重复。
- 无综合题的三个 Lab 明确说明源文档未提供综合题，不补造占位题。

### R3. 题库数据与展示合同

- 为每道选择题保留跨 Lab 唯一且稳定的内部 `id`，并正确映射 `answer` 为 `0`～`3`；删除可选的 `source` 与 `targetId` 字段，避免页面显示来源和题目标识。
- 选项去掉源文档中的 `A.`～`D.` 前缀；Markdown 公式、表格、代码与层级文本保持可渲染。
- 三份源文档引用同一张外部树图；入库时使用源题已经提供的文字结构描述保证独立可答，不热链或复制外部图片。
- 综合题使用 VitePress 原生标题与折叠块呈现题面和参考答案，移除可见来源信息并避免把答案默认展开。

### R4. 目录接入与范围控制

- 在 `content/chapter-04-tree/00-overview.md` 增加“配套理论题”目录，列出八个 Lab、主题与题量，并使用相对 `.md` 链接。
- 新 Quiz 的 `lab.json` 让现有 `CourseIndex` 自动归类到第 4 章“理论 Theory”；不手工修改侧栏、Labs 首页或第二份导航清单。
- 不修改 Quiz 组件、Schema、VitePress 数据加载器或 Pages 发布架构。
- 不修改既有两篇第 4 章教材正文和 14 个既有 Program Lab 的知识内容。
- 内容状态保持 `draft`，等待 Review Owner 独立核对题面、答案和解析。

### R5. 本地验收、提交与 PR 边界

- 运行目标 Lab 校验、内容校验、类型检查、lint、自动发现、生产构建、产物审计和全量 `pnpm test`。
- 在 Pages base 环境运行构建、`check:site` 与 `test:pages`；人工检查桌面与 390px 移动端的目录、Lab 路由、选择/提交/反馈/解析/重试以及综合题折叠行为。
- 启动只监听 `127.0.0.1` 的本地站点，把可访问地址交给用户检查。
- 首次交付停在本地预览：不提交、不推送、不开 PR。只有用户确认页面无问题后，才提交、推送工作分支并创建目标为 `chapter/04` 的 PR。

## Acceptance Criteria

- [x] 只处理 `01`～`08` 八份源文档，入库数量严格为 117 道选择题和 16 道综合题，逐文件数量与上表一致。
- [x] 八个 `lab-04-15`～`lab-04-22` 均有 README、`lab.json`、`quiz.json`，order 连续且不与既有 14 个 Lab 冲突。
- [x] 117 道选择题均为四个无字母前缀的非空选项、唯一内部 id、合法答案索引和非空解析；README 没有选择题答案副本。
- [x] 16 道综合题按源顺序出现，题面与参考答案可折叠阅读；无综合题的文档未补造题目。
- [x] 第 4 章概览能导航到全部八个新 Lab，Labs 目录、搜索与侧栏“理论 Theory”分类自动发现这些 Quiz。
- [x] 所有相关本地校验、构建、Pages base 与浏览器交互检查通过，工作树不包含构建产物、缓存、临时脚本或 `09` 之后的源内容。
- [x] 本地预览地址可供用户验收；在用户确认前没有 commit、push 或 PR。
- [ ] PR 最终以 `codex/chapter4-theory-exercises` 为源、`chapter/04` 为目标，并说明 AI 仅用于格式整理，知识正确性仍需人工复核。

## Out of Scope

- `09-森林与二叉树的转换.md`、`10-树和森林的遍历.md`、`11-哈夫曼树与编码.md`、`12-并查集.md`、`13-堆.md`、`98-跨章综合.md` 与 `_目录.md`。
- 新增综合题交互组件、自动评分或保存作答进度。
- 改写、补造、联网搜集或跨文件调剂题目。
- 修改现有 14 个 Program Lab，或恢复旧分支删除的 Huffman Lab。
- 直接推送或改写远端 `chapter/04` 历史，以及在用户验收前提交 PR。

## Open Questions

无。用户已完成本地验收并明确要求提交 PR。
