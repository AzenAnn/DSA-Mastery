# 4.6 二叉树经典问题教学修订

## 目标

依据用户提供的三张审阅截图，澄清 4.6 的宽度定义、展开算法复杂度、LCA 存储前提和路径问题范围，避免读者混用不同定义或算法。用户已要求创建新分支、以 Azen 初始化 Trellis 并完成修改。

## 范围与决策

- 从最新 `origin/main`（1822f6c）创建 `codex/ch04-section-46-clarifications`；开发者使用现有 Azen 身份。
- 主要修改 `content/chapter-04-tree/06-binary-tree-classic-problems.md`，采用轻量 PRD、Codex inline 流程。
- 4.6.1.3 先解释经典宽度（单层实际节点数的最大值），再解释包含中间空位的编号跨度，给出同树对比与各自 BFS 方法；同步学习目标、自测与整数范围前提。
- 4.6.3.2 依据实际代码补充最坏 O(n) 时间、O(1) 空间及摊还分析。截图中的 O(n²) 结论不适用于当前迭代拼接算法；说明递归展开后重复扫描链尾才会出现平方级退化，并修正拼接点被误称为先序末节点的问题。
- 4.6.5.1 明确普通二叉链表没有 parent 指针、p/q 均存在且按节点身份比较；解释从根搜索与自底向上返回，增加示意图、逐步返回过程、最坏 O(n) 时间和 O(h) 栈空间，以及与有父指针时向上跳的关系。
- 4.6.1～4.6.3 分类标题删去冒号后的列举；4.6.4 改为“路径类（根到叶）”，正文链接到 4.6.5.2 的任意端点最大路径和。
- 保持教材文件路径和元数据路由不变；更新修改日期与贡献者。检查已有引用及渲染锚点。

## 验收标准

- [x] 两种宽度定义、不同数值示例、算法与自测一致。
- [x] flatten 复杂度对应页面原代码，含长左链及重复扫描链尾的区别；拼接点解释正确。
- [x] LCA 前提、空返回/单侧返回/双侧返回语义、祖先情况及复杂度完整，图示能解释调用与返回方向。
- [x] 分类标题简洁、根到叶范围明确、最大路径和链接可实际点击。
- [x] 内容校验、类型检查、lint、构建、静态产物审计通过；检查新图、目录及窄屏渲染。
- [x] 通过实际 C++ 示例及独立计数核验关键知识判断，保存审查证据。

## 非目标

不扩展其他章节或 Lab，不更换前端组件/依赖。用户在后续消息中授权提交并创建 PR；合并和发布不在本次范围内。

## 未决问题

无。截图视为审阅材料，技术判断以实际代码、可复现验证和原始来源为准。
