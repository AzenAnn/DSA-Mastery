# 4.6 修订审查与验证记录

日期：2026-09-09。分支：`codex/ch04-section-46-clarifications`，基线：`origin/main` 的 `1822f6c`。开发者：Azen。

## 知识判断

- [x] 经典宽度与 4.3 队列复杂度采用相同定义：单层实际节点数。新增稀疏树的经典宽度为 2，编号跨度为 4。
- [x] [LeetCode 662 原题](https://leetcode.com/problems/maximum-width-of-binary-tree/)明确计算两端非空节点之间的空位，并保证答案在 32 位有符号整数范围内。教材已注明算法对应的定义与数值前提。
- [x] 当前 `flatten` 为迭代拼接，外层逐节点推进，各次前驱搜索的右链不重叠，内层移动合计 O(n)。截图提出的 O(n²) 不适用于此代码；平方级退化对应先递归展开、再逐层重扫链尾的实现。验证中给每个内层扫描目标计数，未出现重复扫描节点。
- [x] 原文将右链拼接点当作先序末节点不成立。反例为左子树根 2 只有左孩子 3：拼接点是 2，先序末节点是 3；后续展开会把 3 插到原右子树之前。
- [x] LCA 明确无 parent 指针、两个目标均存在、按节点指针比较；单侧返回可能仅表示找到一个目标，也可能表示该侧已有 LCA。补充祖先/同节点情况、最坏 O(n) 时间与 O(h) 栈空间。
- [x] 分类标题已简化；4.6.4 明确根到叶范围，指向真实存在的 `max-path-sum` 锚点。学习目标和自测同步使用两种宽度的不同口径。

## 自动验证

| 执行项 | 结果 |
| --- | --- |
| `pnpm run validate` | 通过：82 篇教材、224 个 Lab、219 个 manifest、569 道题；类型检查与 lint 通过 |
| `pnpm run test:discovery` | 通过：含临时内容与分类 Labs 的发现校验、构建，以及移除 fixture 后的最终重建；输出为 `/DSA-Mastery/` base |
| `GITHUB_PAGES_BASE_PATH=/DSA-Mastery` 下的 `pnpm run check:site` | 通过：82 篇教材、224 个 Lab、25 个框架页、369 个 HTML |
| `python work/ch04-46/verify_snippets.py` | 从当前 Markdown 提取并以 C++17 编译 4 个片段；枚举 0～9 节点全部 6,918 种树形，宽度对照独立绝对编号计算、展开结果对照原先序指针序列；512,443 次 LCA 查询对照父表祖先集合，节点值全部重复仍正确 |
| 展开算法额外计数 | 枚举树中每个内层扫描目标至多出现一次，最多移动 7 次；2,000 节点长左链、长右链均移动 0 次，两种宽度均为 1 |
| `node work/ch04-46/check-page.mjs` | 通过：1440/390px × 浅/暗四种环境；标题、两种定义、图像加载、正文链接点击与滚动、页面无横向溢出、浏览器和同源请求无错误 |
| `git diff --check` | 通过 |

临时验证脚本、编译产物、输出与 8 张图示截图保留在被 Git 忽略的 `work/ch04-46/`；没有把验证脚本加入产品测试接口。计数实验是摊还分析的复现佐证，不以有限枚举替代数学证明。

## 人工页面检查与范围核对

- [x] 查看桌面浅色和 390px 深色下的新图截图：宽度图的两个空位清晰，LCA 的孩子实线与返回虚线可区分，浅色画布在暗色主题中可读。
- [x] 浏览器检查脚本按实际页面结构使用外置文档标题、忽略原生锚点的零宽字符，并等待链接的平滑滚动完成；初轮脚本选择器问题已修正，最终四组均通过。没有因此修改前端实现。
- [x] 新图保持纯 `graphviz` fence、稳定 ASCII ID、既有缓存命名方式；仅保留本次两张新图的 SVG。
- [x] 发现测试已自行清理教材和 Lab fixtures；构建触发的其他章节缓存变化已恢复或精确移除。
- [x] 检索 4.6 分类标题和页面引用，未发现其他源文件依赖被修改的标题锚点。文件路径、章节、order、状态和站点路由不变。
- [x] 已按 Trellis update-spec 判断：本次无新 API、内容发现合同、渲染语法或基础设施变更；算法解释保留在教材及本记录中，不另复制到开发规范。

## 交付状态

实现与本任务验证完成。用户后续授权提交 PR，教材和配图已提交为 `3df8149` 并推送，已创建 [PR #159](https://github.com/AzenAnn/DSA-Mastery/pull/159)，Trellis 任务按收尾流程归档。PR 等待独立审阅，未合并或发布。未运行与此次教材编辑无关的全部 Lab 工具测试或全站 Playwright 套件。
