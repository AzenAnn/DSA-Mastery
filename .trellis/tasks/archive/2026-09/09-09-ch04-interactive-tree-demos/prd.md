# 第 4 章树算法交互式演示

## 目标与授权

用户要求新建分支，以 Azen 初始化 Trellis，尽可能落实队员对 4.4、4.5、4.6 的可视化建议，并严格参考 8.1、8.2 的演示格式。原 PR #159 已合并，本任务从最新 origin/main 3435a4c 创建 `codex/ch04-interactive-tree-demos`，覆盖全部四项建议（包括可选 Morris）。用户已授权实现，按仓库 Codex inline 流程执行。

## 范围

1. 4.4 中序线索化：逐步显示 curr/prev、左右指针及 ltag/rtag、访问序列与新建前驱/后继线索，保留首尾 nullptr 边界。
2. 4.4 Morris 中序遍历：显示寻找前驱、建立临时回边、沿回边返回、拆除回边及输出；结束恢复原树。
3. 4.5.3：原树/森林与孩子兄弟二叉树并列，展示加兄弟线、保留首孩子、转换布局；后根/后序森林与二叉树中序同步访问，并可切换先根/前序对应。点击节点显示两侧对应关系。
4. 4.6.3.2：原树与展开过程对照，逐条显示 pred 搜索和指针重连，新增边与划掉的删除边有明确区别；最终顺序符合先序且 left 全空。
5. 统一参考 8.1/8.2 的深色 HTML 演示外观、中文控件、播放/暂停、上一步/下一步、重置、时间线、倍速、预设案例和解说。

## 验收标准

- [x] 四项内容均有真实可操作演示，并嵌入对应教学位置。
- [x] 线索化保留原孩子链接，空域的线索与首尾边界正确；Morris 无遗漏/重复访问并拆净临时指针。
- [x] 两侧遍历独立计算且每次输出同步，支持同一节点两侧高亮和孩子/兄弟映射解释。
- [x] 展开状态真实来自代码操作，边的删改可见，最终先序右链正确；覆盖常见树、偏斜链、单节点及空树。
- [x] 回退/重播/拖动均还原完整状态；切换模式或案例停止旧播放；无自动播放，键盘和减少动态效果设置可用。
- [x] 与参考演示视觉一致，390px 内部及父页面无横向溢出，桌面/手机、站点浅暗主题均可用。
- [x] 根路径及 Pages 子路径加载本地资源，无外部运行依赖；验证内容、类型、lint、算法状态、构建产物和真实 iframe 交互。

## 非目标

不修改 8.1/8.2、不增加 Vue 演示框架或依赖、不扩展 Lab。现有正文和静态图仍可阅读。本次不自动合并或发布；交付可审阅实现与验证证据。

## 未决问题

无；默认采用预设案例，优先把算法关键状态表达清楚。
