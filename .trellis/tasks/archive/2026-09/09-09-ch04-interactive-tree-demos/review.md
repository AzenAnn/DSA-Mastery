# 第 4 章树算法交互式演示 · 完整审查

## 实现范围

分支：`codex/ch04-interactive-tree-demos`，基于已合并 PR #159 的 `origin/main`（3435a4c）。Trellis 开发者为 Azen，按项目 Codex inline 流程完成。

| 教学位置 | 可操作内容 | 核对点 |
| --- | --- | --- |
| 4.4 中序线索化 | curr/prev、递归栈、ltag/rtag、逐条前驱/后继线索 | 输出 D B E A C；原孩子保留；首尾 null 仍标 Thread |
| 4.4 Morris 拓展 | pred 搜索、临时回边建立/返回/拆除 | 结束指针完全还原、临时回边为 0 |
| 4.5.3 遍历等价 | 加兄弟线、保留首孩子、转换布局、双侧同步访问及节点选中 | 后根/森林后序对应中序，先根/森林先序对应前序 |
| 4.6.3.2 先序展开 | 原树对照、pred 搜索、三次赋值独立展示、删边/加边 | 先序右链、left 全空、拼接点可有左孩子、长左链 pred 移动为 0 |

## 审查结果

- [PASS] 读取并沿用 8.1/8.2 独立 HTML 的深色 hero/card、配色、原生中文控件和 iframe 局部样式；未修改参考演示和全局主题。
- [PASS] 算法轨迹来自实际指针操作；Node 枚举 0–7 节点全部 626 种二叉树，验证永久线索前向/反向遍历、原孩子不变、Morris 还原、flatten 先序及扫描计数。
- [PASS] 626 种有序森林往返孩子兄弟转换，并用独立树/二叉树遍历核对两种对应关系；每帧的两侧输出前缀一致。
- [PASS] 深拷贝帧隔离后续修改；flatten 三次赋值分别成帧，重合 L/R 用不同路径显示；删边有红虚线与叉号；Morris 删除回边保留弧线路径。
- [PASS] 案例覆盖课本树、分支树、拼接点仍有左孩子、左链/右链、单节点、空树、多根森林；教材提供明确观察目标。
- [PASS] 新增 19 项最终教材 iframe 浏览器测试，根路径全部通过：步进/回退/拖动、暂停与旧计时器取消、重播、案例/模式切换、空结构、跨图节点对应与键盘焦点。
- [PASS] 390px/1440px、站点浅暗主题与 reduced-motion 设置下无父页面或 iframe 横向溢出，页面无控制台错误、同源失败请求或缺失资源。
- [PASS] 模块使用 HTML 相对资源路径，教材使用 withBase；没有引入运行依赖、CDN 或硬编码部署前缀。
- [PASS] 根路径 `pnpm test` 完整通过：内容/类型/lint；5 项树演示测试；36 项启动器测试；Lab 工具 40 通过、1 项平台条件跳过；Lab 文档、自动发现、最终构建与产物审计。
- [PASS] Pages 子路径构建与产物审计通过（base=/DSA-Mastery/）；全站 44 项 Playwright 测试全部通过，含新加入的 19 项树演示检查。

## 验证证据

- `work/ch04-interactive/test-root.log`：根路径完整检查通过；373 个 HTML、82 篇教材、224 个 Lab、25 个课程框架页。
- `work/ch04-interactive/browser-root.log`：19 passed。
- `work/ch04-interactive/build-pages.log`、`check-pages.log`、`browser-pages.log`：Pages 构建与审计通过；44 passed（3.0m）。
- 人工检查桌面/手机独立演示，以及实际教材中的 `page-forest-1440-light.png`、`iframe-morris-390-light.png`、`iframe-flatten-390-dark.png` 等截图。截图与日志保留在被忽略的 work 目录，不作为产品资产提交。
- 初次 lint 报测试浏览器 globals 未声明，已改为 globalThis 并复查通过。一次截图捕获出现临时 Chromium 错误，重跑成功，页面本身无错误。

## 维护与交付边界

纯轨迹、视图控制和样式分为三个共享文件；19 项浏览器用例复用现有 Pages 服务器，增加 .mjs MIME；算法门禁已加入 pnpm test 和 Pages workflow。独立演示合同已进入前端规范，作者指南与验证命令说明已同步。

演示使用预设案例，原理论与静态图保持完整。双侧同步以输出事件为单位，并非递归调用逐帧一一对应。演示快照的内存开销与原算法空间复杂度分别说明。实现提交为 9ee5578；用户已授权提交本分支 PR。外部审阅与合并状态以 GitHub PR 为准。

已恢复构建器删除的原有缓存，并按精确路径移除三份无关新缓存；git diff --check 通过。临时截图/测试服务器均已关闭。另保留端口 4174 的本地预览供用户查看（运行会话 90690），不属于部署。
