# 实施计划：第 5 章树的应用内容修正与教学增强

按「先修事实错误、再补连接、最后做教学增强」的顺序推进，每步可独立验证。

## 阶段 1：事实性修正（P0）

**目标**：读者不再被错误描述误导。

1. 修正 `00-overview.md` 的 `::: info` 容器：
   - Exercise 改为如实描述 17 个实验题及其主题分布；
   - Theory 保持 5 组的现有描述；
   - Project 保持“空槽位”（确为空）。

**验证**：描述与 `ls labs/chapter-05/*/` 输出逐项核对。

## 阶段 2：配套 Lab 连接（P0）

2. `00-overview.md` 补全章 Lab 汇总表（Theory 5 组 + Exercise 17 题）。
3. 五篇正文各补 `## 配套 Lab` 段落，位置在「小结与自测」之前：
   - 5.1 → E-05-01 ~ E-05-05；
   - 5.2 → T-05-05 + E-05-06 ~ E-05-08；
   - 5.3 → T-05-03 + E-05-09 ~ E-05-11；
   - 5.4 → T-05-04 + E-05-12 ~ E-05-15；
   - 5.5 → E-05-16、E-05-17。

**验证**：`pnpm run validate:content`；预览时逐个点击链接。

**注意**：`T-05-01`、`T-05-02` 按非目标跳过，不引用。

## 阶段 3：教学增强（P1）

4. **5.5 B 树分步表**：插入 `10,20,5,6,12,30,7,17` 的 8 步演算，标出插入 `6` 与 `17` 时的两次分裂；终态须与既有示例一致。
5. **5.1 失衡与旋转图**：补 LR 双旋两步图与四类失衡形态图。
6. **5.5 B 树代码骨架**：节点定义 + 查找实现；分裂完整实现指向 `E-05-16`。
7. **25 道自测题参考答案**：六篇逐题补 `::: details 参考答案`；5.2 第 5 题改为反例问法。

**验证**：所有计算题与证明题人工独立验算一遍，不采信未经核对的结果。

## 阶段 4：一致性（P2）

8. **图示改造**：按 design.md 的清单把 5.1、5.4、5.5 的结构图改为 `graphviz` 并补 `caption`；保留 5.2 堆数组图与 5.3 编解码轨迹的 ASCII。
9. **`#include` 统一**：5.2 前两个代码块补齐所需头文件；核对其余代码块。
10. **五结构横向对比表**：在 5.5 末尾补表，兑现 overview 的三问框架。

## 阶段 5：验证与预览

11. `pnpm run validate:content`。
12. `pnpm run dev` 启动本地预览，交付给维护者检查。
13. 双主题 × 双宽度（390px / 1440px）检查图表与表格。
14. `git diff --stat` 确认只改动 `content/chapter-05-tree-applications/` 下六个文件。

## 交付边界

- 不暂存、不提交、不 push；由维护者审查后自行执行。
- 完成后用 `add_session.py --no-commit` 记录会话。
- 如实记录实际执行的命令与结果，未运行的检查不写“通过”。
