# PRD：补全线性表第 3、4 篇文章

## Goal

按照用户提供的章节框架，把线性表中的第 3 篇“第二种实现——链表与演进设计”和第 4 篇“比较与权衡”建设成可连续阅读、可实践、可本地预览的完整中文教材。读者不仅要会写基本操作，还要理解链表设计为何逐步引入封装、尺寸缓存、哨兵和双向链接，并能根据访问模式、缓存局部性与空间成本选择顺序表或链表。

## Background and Confirmed Facts

- 参考图明确给出了第 3 篇的 3.1～3.4 框架和第 4 篇的 4.1～4.2 框架。
- 当前 `content/chapter-01-linear-list/03-linked-list.md` 只有基础骨架；第 4 篇尚不存在。
- 项目教材使用中文与 C++ 示例，VitePress 已原生支持代码高亮、行号、callout、表格、公式、目录与折叠答案。
- 教材页面从 Markdown 自动发现，但课程侧栏还要通过 `.vitepress/content-index.ts` 的课程编排来源映射；其中线性表仍指向未落地的旧拆分文件。
- 当前分支为 `codex/linear-list-articles-3-4`，Trellis creator/assignee 均为 `Azen`。

## Requirements

### R1. 第 3 篇：链表与演进设计

- 保留兼容 URL 对应的源文件 `03-linked-list.md`，标题升级为“1.3 第二种实现——链表与演进设计”。
- 完整覆盖参考框架：
  - 3.1 离散存储引入：顺序表批量平移的 `O(n)` 痛点；节点通过 `next` 建立逻辑链条。
  - 3.2 坏设计的重构路线：裸节点链表、封装单链表、`size` 缓存。
  - 3.3 哨兵节点：表头特判问题与 dummy head 的统一边界。
  - 3.4 双向链表：单链表尾删限制、`prev`/`next`、双向循环带哨兵链表、定位成本与修改成本的区分。
- 给出可追踪指针变化的图示或文本链路、复杂度表、常见错误和不变量。
- 至少包含 3 道有完整推导或折叠答案的例题，并提供由浅入深的自测题。
- 提供可编译的 C++ 核心实现片段，逐段解释操作前置条件、指针更新顺序、`size` 维护、析构/所有权边界和复杂度；正文不复制 Lab 的完整工程结构。

### R2. 第 4 篇：比较与权衡

- 新增 `04-comparison-and-selection.md`，标题为“1.4 比较与权衡”。
- 完整覆盖参考框架：
  - 4.1 随机访问、批量移动/局部修改、CPU Cache Line 与内存连续性、空间利用率的对比矩阵。
  - 4.2 “读多写少”和“频繁头尾增删”两类选型路径。
- 明确区分“找到位置”与“修改结构”，避免把链表插删笼统写成 `O(1)`。
- 将“8～16 字节/节点”等空间描述标注为与指针宽度、对齐和分配器有关的典型量级，不写成跨平台定值。
- 至少包含 3 个带分析答案的场景例题；覆盖读密集数据、稳定节点/局部修改、队列或撤销历史、LRU 等组合结构，并说明 `std::vector`、`std::list`、`std::deque` 不是只按复杂度表机械选择。
- 提供 C++ 示例和代码讲解，展示如何把操作频率和稳定引用需求转化为选型，而非进行不可复现的绝对性能宣称。

### R3. 课程接入

- 更新章首页导览，使第 3、4 篇都可从 `00-overview.md` 进入。
- 把 `.vitepress/content-index.ts` 中线性表的课程来源映射对齐为实际的 `00`～`04` 页面，移除该位置尚不存在的旧拆分路径。
- 更新 `content/README.md` 的线性表目录示例，保持写作者文档与事实来源一致。
- 不新增第二份导航、不手写 Pages base、不修改路由规则、主题组件或样式。

### R4. 元数据与状态

- 两篇文章 frontmatter 遵守章节、文件序号和字段契约；`updated` 使用 `2026-08-13`，`contributors` 记录 `Azen`。
- 内容保持 `status: "draft"`，本次自动验证和本地预览不冒充独立知识审阅或发布批准。

### R5. 验证与本地预览

- 代表性 C++ 示例使用本机 `g++` 或 `clang++` 以 C++17 编译，并运行断言示例。
- 运行内容、类型、lint、自动发现、构建、静态产物和 Pages-base 浏览器门禁；失败必须修复或明确记录。
- 浏览器检查第 3、4 篇的侧栏、前后篇、目录、代码块、表格、折叠答案、桌面和移动阅读效果，且无同源请求、控制台或页面错误。
- 启动一个绑定 `127.0.0.1` 的本地预览服务，保持到用户检查，并提供第 3、4 篇的直接访问地址。

## Acceptance Criteria

- [x] 第 3 篇包含 3.1～3.4 的全部主题、至少 3 道讲解例题、可编译 C++ 核心代码、复杂度与易错点说明。
- [x] 第 4 篇包含 4.1～4.2 的全部主题、至少 3 道场景例题、C++ 选型示例，并正确说明缓存与空间结论的适用条件。
- [x] 章首页、课程框架、侧栏、搜索和 prev/next 能一致发现并导航到第 3、4 篇。
- [x] 两篇 frontmatter、相对链接、标题层级和代码围栏通过内容与构建校验。
- [x] 代表性 C++ 示例编译并运行通过。
- [x] `pnpm test` 通过；Pages base 下的 `build`、`check:site` 与 `test:pages` 通过。
- [x] 本地预览可访问，用户能直接打开两篇文章检查渲染效果。

## Out of Scope

- 不重写第 1、2 篇文章，不扩建或重写现有 Lab。
- 不新增 Vue 组件、交互实验、主题样式、依赖或路由机制。
- 不把环境相关的微基准结果包装成固定结论。
- 本次不提交、不推送、不开 PR、不发布到 GitHub Pages；由用户预览后决定后续动作。

## Open Questions

无阻塞问题。文章框架、语言、状态、导航方式和预览目标均已由用户输入与仓库规范确定。
