const lc = (id, slug) => ({ source: `LeetCode ${id}`, sourceUrl: `https://leetcode.com/problems/${slug}/`, adaptation: '保留原题图语义，将函数参数与返回值改为标准输入输出。' })
const course = (section) => ({ source: `DSA Mastery 6.${section}`, sourceUrl: `https://azenann.github.io/DSA-Mastery/learn/chapter-06-graph-foundations/0${section}-${section === 2 ? 'graph-storage' : 'graph-traversal-connectivity'}/`, adaptation: '本题为根据课程知识点设计的独立练习，输入输出和数据范围由本 Lab 定义。' })
const cses = (id) => ({ source: `CSES ${id}`, sourceUrl: `https://cses.fi/problemset/task/${id}/`, adaptation: '本 Lab 使用下述本地数据范围与规范化输出。原题允许任意有效答案，本地评分按明确的规范化规则比较；样例为课程样例。' })

export const catalog = [
  {
    id: 4, slug: 'star-graph-center', title: '星型图的中心', ...lc(1791, 'find-center-of-star-graph'), article: 1,
    description: '利用星型图中任意两条边的公共端点确定中心。',
    statement: '一张星型无向图有 n 个顶点：存在一个中心与其余 n-1 个顶点分别相连，没有其他边。边的输入顺序和每条边的端点顺序均不固定，请输出唯一的中心编号。输入保证是合法星型图，无需检验图形是否合法。',
    input: '第一行一个整数 `n`；随后 `n-1` 行，每行两个整数 `u v` 表示一条无向边。',
    output: '输出一个整数，表示中心顶点编号。',
    constraints: '`3 <= n <= 100000`，顶点编号 `1..n`。无自环、无重边，恰有 `n-1` 条边。',
    steps: ['找出前两条边的公共端点，并考虑该端点出现在不同位置的情况。', '继续读取剩余输入，最后只输出中心编号。'],
    proof: '星型图的每条边都包含中心，而不同叶子之间没有边，因此两条不同边的唯一公共端点必为中心。只使用前两条边就足够确定答案。',
    complexity: '读完输入需要 `O(n)` 时间，识别中心只需 `O(1)` 次比较；参考实现额外空间 `O(1)`。使用度数数组也能完成，但会增加到 `O(n)` 空间。',
    pitfall: '不能假设中心总在第一个端点位置；最小合法规模是 3，不是 2。',
    boundaries: ['中心编号为 1 或 n：仍按公共端点判断。', '两条边的端点方向相反：无向边的顺序不影响结果。'],
    sampleExplanation: '三条边都包含顶点 2，其他顶点只与 2 相连，所以中心为 2。',
    questions: ['为什么必须保证输入是星型图？仅凭两条边能否验证整张图为星型图？', '使用度数统计时，如何判断中心？额外存储代价是多少？']
  },
  {
    id: 5, slug: 'town-judge', title: '小镇法官', ...lc(997, 'find-the-town-judge'), article: 1,
    description: '用入度和出度同时刻画“被所有其他人信任且不信任任何人”。',
    statement: '小镇有 n 个人。每条信任记录 u v 表示 u 信任 v。若存在一个人不信任任何人，且其余 n-1 人都信任他，则他是法官。请返回法官编号；不存在则输出 -1。信任不是双向关系，也不具有传递性。',
    input: '第一行 `n m`，随后 `m` 行为信任记录 `u v`。', output: '输出法官编号；不存在时输出 `-1`。',
    constraints: '`1 <= n <= 1000`，`0 <= m <= 10000`，顶点编号 `1..n`；`u != v`，每个有序对最多出现一次。',
    steps: ['记录每个人直接信任他人的次数和被直接信任的次数。', '同时检查两个条件，保留不存在法官时的 -1。'],
    proof: '出度为 0 等价于不信任任何人；在无自环且有序对不重复的条件下，入度为 n-1 等价于其余所有人都信任该人。两个条件同时成立正好与法官定义等价。',
    complexity: '时间 `O(n+m)`，额外空间 `O(n)`；不需要邻接矩阵。',
    pitfall: '只看入度会把仍信任别人的人判为法官；相互信任不能合并成无向边。',
    boundaries: ['n=1、m=0：唯一的人满足条件，输出 1。', 'n>1、m=0：没有人得到全部信任，输出 -1。'],
    sampleExplanation: '1 和 2 都信任 3，3 没有向外的信任记录，因此法官是 3。',
    questions: ['为什么“入度减出度等于 n-1”也能判定本题法官？这一判断依赖哪些输入限制？', '如果允许重复的信任记录，直接统计入度还可靠吗？']
  },
  {
    id: 6, slug: 'adjacency-matrix-queries', title: '邻接矩阵建图与相邻查询', source: '洛谷 T471612', sourceUrl: 'https://www.luogu.com.cn/problem/T471612', article: 2,
    adaptation: '原题“图的存储 (邻接矩阵)”要求输出矩阵和邻接表。本 Lab 保留矩阵输出，增加相邻查询，省略邻接表输出，并允许空图。',
    description: '构造对称邻接矩阵，并用常数时间回答两点是否直接相邻。',
    statement: '给定简单无向图，先输出它的邻接矩阵，再回答 q 次查询：u 与 v 之间是否存在一条直接连接的边？相邻只指直接有边，不等于通过其他顶点可达。',
    input: '第一行 `n m q`；随后 `m` 行为无向边 `u v`；最后 `q` 行为查询 `u v`。',
    output: '先输出 `n` 行，每行 `n` 个 0/1，行列按顶点 `1..n` 排列。然后输出 `q` 行，每行是对应查询的结果：有边为 `1`，无边为 `0`。',
    constraints: '`1 <= n <= 1000`，`0 <= m <= min(100000,n(n-1)/2)`，`0 <= q <= 100000`。顶点编号 `1..n`，图无自环重边；查询允许 `u=v`。',
    steps: ['将矩阵初始化为 0，每条无向边更新两个对称单元。', '按行输出矩阵，再直接索引矩阵回答查询。'],
    proof: '初始化时没有边，所有单元为 0。每次读入 {u,v}，仅将 A[u][v] 和 A[v][u] 置 1，因此读完后单元为 1 当且仅当对应边存在。查询正是读取这个定义。',
    complexity: '时间 `O(n^2+m+q)`，空间 `O(n^2)`；其中矩阵输出本身就需要 `O(n^2)` 时间，每次查询 `O(1)`。',
    pitfall: '不要把路径可达性当相邻关系；输入从 1 开始而数组可能从 0 开始。',
    boundaries: ['m=0：矩阵全为 0。', '查询同一顶点：无自环图的结果为 0。', 'q=0：仅输出矩阵。'],
    sampleExplanation: '1 与 2 直接相邻，但 1 与 3 仅能经 2 到达。查询 1 3 的结果是 0，反向查询 2 1 的结果是 1。',
    questions: ['为什么 n 很大但 m 很小时不适合分配矩阵？', '有向图的建图语句与这里有什么区别？']
  },
  {
    id: 7, slug: 'adjacency-list-normalization', title: '邻接表的构建与规范化输出', source: '洛谷 T419391', sourceUrl: 'https://www.luogu.com.cn/problem/T419391', article: 2,
    adaptation: '原题“【数据结构】图的存储”同时输出矩阵和邻接表。本 Lab 专练邻接表，省略矩阵输出、允许空图，并将 n 上限扩大到 100000。',
    description: '在稀疏图中构建双向邻接记录，并输出排序后的邻接表。',
    statement: '给定一张简单无向图。请按顶点编号递增输出每个顶点的度数及全部邻居，邻居必须按编号递增排列，输出不得依赖输入边的先后顺序。',
    input: '第一行 `n m`，随后 `m` 行给出无向边 `u v`。',
    output: '输出 `n` 行，第 i 行先输出顶点 i 的度数 `k`，再输出 k 个升序邻居。孤立点单独输出 `0`。',
    constraints: '`1 <= n <= 100000`，`0 <= m <= min(200000,n(n-1)/2)`，顶点编号 `1..n`；无自环、无重边。',
    steps: ['为每条无向边写入两个邻接记录。', '分别排序每个顶点的邻居，并以“数量 + 内容”输出，包括空邻接表。'],
    proof: '每条边在两个端点处分别登记，所以每个顶点的列表恰好包含其邻居。简单图没有重复邻居，列表长度就是度。排序只改变顺序，不改变图。',
    complexity: '时间 `O(n+m+sum(d_v log(d_v+1)))`，其中 d_v 为顶点 v 的度；空间 `O(n+m)`，邻接记录总数为 `2m`。',
    pitfall: '输入边未排序；仅输出出现过的顶点会漏掉孤立点。',
    boundaries: ['孤立点：该行仍必须存在，内容为 0。', '高编号顶点或星型中心：邻居数量可能远大于其他点。'],
    sampleExplanation: '顶点 3 与 1、2、4、5 相邻，虽然输入顺序不同，其输出仍为 4 1 2 4 5。',
    questions: ['为什么不能声称包含排序的整个过程始终是 O(n+m)？', '若无向自环也被允许，列表长度与图论中的度还能直接等同吗？']
  },
  {
    id: 8, slug: 'directed-graph-transpose', title: '有向图的转置', ...course(3), article: 3,
    description: '反转全部有向边，在规范化输出中保留自环和平行边。',
    statement: '有向图的转置图保持顶点集不变，将每条 u 到 v 的边替换为 v 到 u。输入可能包含自环和平行边，每条输入边都要对应一条转置边。请输出转置图的邻接表。',
    input: '第一行 `n m`，随后 `m` 行为有向边 `u v`。',
    output: '输出 `n` 行，依次对应 `0..n-1`。每行先输出出边条数 k，再输出 k 个升序终点。平行边的终点应重复输出；无出边时输出 `0`。',
    constraints: '`1 <= n <= 100000`，`0 <= m <= 200000`，顶点编号 `0..n-1`；允许自环、平行边。',
    steps: ['逐条反转输入边并插入新的邻接表。', '排序各出边列表，但不要去重；核对转置前后边数相同。'],
    proof: '映射 (u,v) 到 (v,u) 是双射，连续转置两次恢复原边，因此不会丢失或增加边。自环仍映射到自身，多条相同边也逐条保留。',
    complexity: '建图时间 `O(n+m)`，排序后总时间 `O(n+m+sum(d_v log(d_v+1)))`，d_v 是转置图出度；空间 `O(n+m)`。',
    pitfall: '转置不等于把图变无向；使用 set 会错误丢弃平行边。',
    boundaries: ['空图：输出 n 行 0。', '自环：转置后仍保留。', '重边：重复终点不能删去。'],
    sampleExplanation: '输入中有两条 0 到 1 的边，转置后顶点 1 的列表中有两个 0；1 到 1 的自环仍在该列表中。',
    questions: ['转置图的出度与原图的哪一种度相同？', '为什么在原邻接表上边遍历边追加反向边容易出错？']
  },
  {
    id: 9, slug: 'graph-representation-conversion', title: '图的三种表示转换', ...course(2), article: 2, difficulty: '进阶',
    description: '从边集、矩阵或邻接表读入同一张图，并输出三种规范表示。',
    statement: '给定一张无权简单图及其一种存储表示。图可能有向或无向，输入表示可能是边集 E、邻接矩阵 M 或邻接表 L。请输出该图的三种标准表示，使结果与输入表示及输入边顺序无关。',
    input: '首行 `type format n`，type 为 `U`（无向）或 `D`（有向），format 为 `E`、`M` 或 `L`。\n\n- E：下一行 m，随后 m 行 `u v`；无向边仅输入一次，端点顺序任意。\n- M：接下来 n 行，每行 n 个 0/1；U 的矩阵保证对称。\n- L：接下来 n 行，对应顶点 0..n-1；每行 k 加 k 个互异邻居，邻居可乱序。U 的邻接记录保证双向一致。',
    output: '依次输出三个区段：\n\n1. 一行 `MATRIX`，再输出 n 行 n 列矩阵。\n2. 一行 `LIST`，再输出 n 行“邻居数量 + 升序邻居”，无邻居输出 0。\n3. 一行 `EDGES m`，再输出 m 行边；按起点升序、起点相同时按终点升序。U 仅输出 `u<v` 的逻辑边，D 输出全部有向边。',
    constraints: '`1 <= n <= 500`，顶点编号 `0..n-1`；无自环、无重边，矩阵对角线为 0。U 有 `0 <= m <= n(n-1)/2` 条逻辑边，D 有 `0 <= m <= n(n-1)` 条边。三种输入都保证有效。',
    steps: ['根据 format 解码输入，注意无向边在 E 中一次、在 M/L 中双向出现。', '生成矩阵和升序邻接表，再枚举规范边集，防止无向边重复计数。'],
    proof: '可以将“是否存在 u 到 v 的边”作为共同状态。三种输入的每项都准确还原这一关系，再按固定的顶点顺序枚举该关系，所得三种输出描述相同的图。',
    complexity: '时间 `O(n^2+m)`，空间 `O(n^2+m)`；参考实现按矩阵行列递增枚举，因此邻接表和边集无需额外排序。',
    pitfall: '无向边的逻辑条数 m 不等于邻接记录总数 2m；D 不能强制补反向边。',
    boundaries: ['n=1 或空图：矩阵为 0，邻接表为空，EDGES 后的计数为 0。', '同一图的 E/M/L 输入：三个区段的输出必须一致。'],
    sampleExplanation: '边集输入两条无向边，矩阵写入四个 1；邻接表共有四条记录，但 EDGES 区段仍只有两条边，并统一输出较小端点在前。',
    questions: ['只把 E 转成 M/L 是否完成本题？另外两种输入需要处理什么？', '若引入权值 0，为什么不能再直接用矩阵 0 表示“没有边”？']
  },
  {
    id: 10, slug: 'graph-adt', title: 'Graph ADT：边的增删查', ...course(2), article: 2,
    description: '封装简单图的动态边操作，并维护无向图的双向一致性。',
    statement: '图最初有 n 个顶点而没有边。依次处理 ADD、DEL、HAS 三类操作；图类型在整个输入中不变。重复添加不能产生重边，删除不存在的边不改变图。',
    input: '首行 `type n q`，type 为 U 或 D。随后 q 行，每行命令 `ADD u v`、`DEL u v` 或 `HAS u v`。',
    output: '每条操作输出一行 0/1：ADD 新增边返回 1、原已存在返回 0；DEL 删除已有边返回 1、边不存在返回 0；HAS 存在返回 1，否则返回 0。q=0 时无输出。',
    constraints: '`1 <= n <= 100000`，`0 <= q <= 200000`，顶点编号 `0..n-1`，每条操作 `u!=v`；命令和编号保证合法。U 中 {u,v} 与 {v,u} 是同一条边；D 中两者独立。',
    steps: ['实现 add、remove、has 三个操作，返回值反映当前操作的效果。', '确保 U 在两个方向同步增删，D 只修改指定方向。'],
    proof: '初始空图满足简单性与无向对称性。ADD 使用集合确保唯一性，DEL 删除集合中已有元素；U 对两端执行相同变更，维持对称关系。HAS 只读，所以这些不变量在所有操作后仍成立。',
    complexity: '使用每个顶点的有序集合，单次操作 `O(log(n+1))`，总时间 `O(n+q log(n+1))`，空间 `O(n+e)`，e 为当前边数。',
    pitfall: 'ADD/DEL 的返回值是“是否产生变更”，不是操作结束后边是否存在。',
    boundaries: ['反向重复 ADD：U 返回 0，D 可返回 1。', '连续 DEL 同一条边：首次成功后再删返回 0。', 'q=0：不要输出额外的 0。'],
    sampleExplanation: '首次 ADD 0 1 成功，HAS 1 0 在无向图中为真；再次反向添加不改变图。反向删除后，两种方向的查询都应为假。',
    questions: ['用 vector 保存邻居时，删除操作的复杂度如何变化？', '为什么 has 应当是 const 成员函数？']
  },
  {
    id: 11, slug: 'maximal-network-rank', title: '最大网络秩', ...lc(1615, 'maximal-network-rank'), article: 1,
    description: '枚举城市对，用度数求道路并集大小，并消除共享道路的重复计数。',
    statement: 'n 座城市之间存在若干双向道路，任意两座城市最多一条道路。两座不同城市的网络秩是至少与其中一座城市相连的道路总数，它们之间的直接道路只计算一次。求所有城市对中的最大网络秩。',
    input: '第一行 `n m`，随后 m 行为无向道路 `u v`。', output: '输出最大网络秩。',
    constraints: '`2 <= n <= 100`，`0 <= m <= n(n-1)/2`，顶点编号 `0..n-1`；无自环重边。',
    steps: ['计算度数并记录相邻关系。', '枚举 u<v，求两座城市关联道路的并集大小并维护最大值。'],
    proof: 'degree[u]+degree[v] 对两城之间的道路计了两次，对其他关联道路只计一次，因此存在直接道路时减 1，否则不减。枚举全部不同城市对即可得到最大值。',
    complexity: '时间 `O(n^2+m)`，空间 `O(n^2)`。',
    pitfall: '不能总是减 1，也不能把“最高度数的两个编号”未经并列分析就当成最优组合。',
    boundaries: ['m=0：答案为 0。', '只有两城一条路：答案为 1，不是 2。'],
    sampleExplanation: '城市 1 的度为 3，城市 0 和 3 的度都为 2；选择 0、1 时共享的 0-1 道路只计一次，网络秩为 4。',
    questions: ['为什么只需枚举 u<v？', '最高度数有多人并列时，相邻关系会如何影响答案？']
  },
  {
    id: 12, slug: 'valid-graph-path', title: '寻找图中是否存在路径', ...lc(1971, 'find-if-path-exists-in-graph'), article: 3,
    description: '在无向图中进行单源遍历，判断终点是否属于起点的连通分量。',
    statement: '给定简单无向图和两个顶点 source、destination，判断是否能沿图中的边从起点到达终点。允许长度为 0 的路径，即顶点总能到达自身。',
    input: '第一行 `n m`，随后 m 行无向边 `u v`，最后一行 `source destination`。', output: '存在路径输出 `1`，否则输出 `0`。',
    constraints: '`1 <= n <= 200000`，`0 <= m <= 200000`，顶点编号和起终点均在 `0..n-1`；无自环重边。',
    steps: ['从 source 开始 DFS 或 BFS，在顶点加入待处理集合时标记。', '检查 destination 是否被访问；无需输出实际路径。'],
    proof: '被发现的每个顶点都由已可达顶点沿一条边进入，因此不会标记不可达点。反过来，对任意一条从起点出发的路径逐边归纳，路径上所有点都会被发现，故标记集合恰好是可达集合。',
    complexity: '时间 `O(n+m)`，空间 `O(n+m)`；参考实现使用显式栈以支持长链。',
    pitfall: '起点必须先标记；只检查是否直接有边不能判断路径。',
    boundaries: ['source=destination：即使 m=0 也输出 1。', '起终点在不同分量：输出 0。', '环：visited 必须防止重复处理。'],
    sampleExplanation: '0 与 2 之间存在直接边，也可经 1 到达，因此输出 1。',
    questions: ['能否在首次发现终点时提前结束遍历？', '如果有很多次静态连通查询，先标号所有分量会带来什么收益？']
  },
  {
    id: 13, slug: 'keys-and-rooms', title: '钥匙和房间', ...lc(841, 'keys-and-rooms'), article: 3,
    description: '把钥匙看作有向边，区分拥有钥匙与实际能够取得钥匙。',
    statement: '有 n 个房间，最初只有房间 0 打开。进入房间后可以取得其中所有钥匙，每把编号为 v 的钥匙可打开房间 v。每间房里的钥匙可任意使用，不会消耗。判断能否进入全部房间。',
    input: '第一行 n。随后 n 行对应房间 0..n-1，每行先给钥匙数 k，再给 k 个钥匙编号。空钥匙列表写作 `0`。',
    output: '可以访问全部房间输出 `1`，否则输出 `0`。',
    constraints: '`2 <= n <= 1000`，每间房 `0 <= k <= min(n,1000)`，总钥匙数 `1..3000`。钥匙编号 `0..n-1`；同一房间内编号互异，不同房间可有相同钥匙，允许指向本房间的钥匙。',
    steps: ['将每个房间的钥匙作为该顶点的有向出边。', '仅从房间 0 遍历，统计不同的已访问房间数量。'],
    proof: '只有进入房间后才能取得它的钥匙，所以可进入的房间恰好是从 0 沿钥匙有向边可达的顶点。visited 保证重复钥匙和自钥匙不会重复计数。',
    complexity: '设总钥匙数为 K，时间和空间均为 `O(n+K)`。',
    pitfall: '不能从每个未访问房间启动新遍历；那会把原本锁住且无钥匙可达的房间也算入。',
    boundaries: ['房间 0 没有钥匙：n>=2，因此输出 0。', '未开放房间之间形成钥匙环：它们仍可能无法进入。', '本房间钥匙：不应死循环或增加访问数量。'],
    sampleExplanation: '依次进入 0、1、2、3，沿途拿到下一间房的钥匙，因此所有房间都能访问。',
    questions: ['为什么把钥匙关系当成无向边会导致错误？', '“每间房的钥匙编号都在某处出现”是否足以保证全部房间可进入？']
  },
  {
    id: 14, slug: 'number-of-provinces', title: '省份数量', ...lc(547, 'number-of-provinces'), article: 3,
    description: '从对称连接矩阵出发，统计包含孤立城市的全部连通分量。',
    statement: '若两座城市直接相连，或能通过其他城市间接相连，则它们属于同一个省份。给定直接连接矩阵，请输出省份总数；每座城市恰属于一个省份。',
    input: '第一行 n；接下来 n 行，每行 n 个 0/1，矩阵元素 A[i][j] 表示城市 i 与 j 是否直接相连。', output: '输出省份总数。',
    constraints: '`1 <= n <= 200`；矩阵为 n 行 n 列、对称且主对角线为 1，城市内部编号 `0..n-1`。',
    steps: ['扫描所有城市，遇到尚未访问的城市时建立一个新省份。', '从该城市遍历矩阵中的邻居，标记整个省份后再继续外层扫描。'],
    proof: '每次遍历恰好访问一个连通分量。该分量内的其他城市已被标记，不会再次增加计数；所有城市最终都会被外层扫描覆盖，因此计数正好等于省份数。',
    complexity: '时间 `O(n^2)`，输入矩阵空间 `O(n^2)`，额外遍历空间 `O(n)`。',
    pitfall: '只数矩阵中的 1 或只统计直接连接组都不够；间接连接也属于同一省份。',
    boundaries: ['n=1：矩阵为 [1]，输出 1。', '只有对角线为 1：n 个省份。', '所有元素均为 1：只有 1 个省份。'],
    sampleExplanation: '城市 0、1 相连，城市 2 只与自身相连，因此共有 2 个省份。',
    questions: ['为什么对角线的 1 不会产生额外省份？', '将矩阵先转成邻接表能否消除读取矩阵所需的 O(n^2) 时间？']
  },
  {
    id: 15, slug: 'connected-component-labels', title: '无向图连通分量标号', ...course(3), article: 3,
    description: '完整遍历无向图，为所有顶点分配稳定的连通分量编号。',
    statement: '给定简单无向图，将互相可达的顶点划入同一连通分量。输出分量数和每个顶点的分量编号。为了使答案唯一，按每个分量中最小顶点的编号从小到大，将分量依次编号为 1..k。',
    input: '第一行 `n m`；随后 m 行无向边 `u v`。',
    output: '第一行分量数 k；第二行 n 个整数，按顶点 0..n-1 的顺序输出其分量编号。编号必须满足上述最小顶点排序规则。',
    constraints: '`1 <= n <= 100000`，`0 <= m <= 200000`，顶点编号 `0..n-1`；无自环重边。',
    steps: ['按顶点递增扫描，对每个未标记顶点启动一次完整分量遍历。', '使用同一编号标记本次遍历所有顶点，再递增分量编号。'],
    proof: '按顶点递增扫描时，每次新遍历的起点恰好是该分量的最小顶点。一次遍历访问且仅访问该分量，所以起点顺序自然给出规范编号。',
    complexity: '时间 `O(n+m)`，空间 `O(n+m)`。',
    pitfall: '编号从 1 开始，顶点从 0 开始；不能按输入边首次出现的顺序给分量编号。',
    boundaries: ['孤立点也有独立编号。', '分量中的顶点可在编号轴上交错，不能按连续区间分组。', '空图的输出标签是 1..n。'],
    sampleExplanation: '三个分量分别为 {0,2,4}、{1,3}、{5}，最小顶点为 0、1、5，因此标签为 1 2 1 2 1 3。',
    questions: ['如果 DFS 邻居遍历顺序改变，规范编号还会改变吗？', '已经保存所有标签后，如何在 O(1) 时间回答两点是否连通？']
  },
  {
    id: 16, slug: 'component-size-statistics', title: '连通分量规模统计', ...course(3), article: 3,
    description: '对每个连通分量统计顶点数，并按规模降序输出。',
    statement: '给定简单无向图，统计所有连通分量的大小。大小指其中不同顶点的数量，不是边数。按非增序输出各分量大小，相同大小需分别保留。',
    input: '第一行 `n m`，随后 m 行无向边 `u v`。', output: '第一行分量数 k；第二行 k 个分量大小，按非增序排列。',
    constraints: '`1 <= n <= 100000`，`0 <= m <= 200000`，顶点编号 `0..n-1`；无自环重边。',
    steps: ['每次新分量遍历时重置本次计数，只统计首次访问的顶点。', '收集全部大小并排序，验证大小总和为 n。'],
    proof: '完整遍历把顶点集划分为不重叠的可达集合，每个顶点只在所属分量中计数一次，所以所有大小之和为 n。排序只重新排列各大小，不改变分量数量或重复大小的重数。',
    complexity: '时间 `O(n+m+k log(k+1))`，空间 `O(n+m)`，k 为分量数。',
    pitfall: '有环时不能在每次遇到顶点就累加；相同大小不能去重。',
    boundaries: ['全是孤立点：输出 n 个 1。', '同样大小的多个分量：保留每一个。', '一个分量：第二行只有 n。'],
    sampleExplanation: '各分量大小为 2、4、2，降序后输出 4 2 2，保留两个大小为 2 的分量。',
    questions: ['为什么边数不能唯一决定分量大小？', '如果只需要最大分量大小，哪些步骤和空间可以省略？']
  },
  {
    id: 17, slug: 'weakly-connected-components', title: '有向图的弱连通分量', ...course(3), article: 3,
    description: '忽略边的方向后计算分量，区分弱连通与有向可达。',
    statement: '给定有向图，将所有边视作无向边后得到底层无向图；它的连通分量称为原图的弱连通分量。输出弱连通分量数和每个顶点的规范标签。标签按各分量最小顶点递增，从 1 开始。',
    input: '第一行 `n m`，随后 m 行有向边 `u v`。', output: '第一行分量数 k，第二行按顶点 0..n-1 输出 n 个规范标签。',
    constraints: '`1 <= n <= 100000`，`0 <= m <= 200000`，顶点编号 `0..n-1`；允许自环和平行边。',
    steps: ['为每条有向边补上可反向遍历的邻接记录，得到底层无向图。', '完整遍历并按最小顶点顺序标号。'],
    proof: '忽略方向后的每条边均可双向经过，与底层无向图的路径定义一致。因此计算该无向图连通分量恰好得到弱连通分量。自环和重边不改变可达集合。',
    complexity: '时间 `O(n+m)`，空间 `O(n+m)`。',
    pitfall: '在原图中仅沿出边遍历不能求弱连通分量；弱连通也不意味着强连通。',
    boundaries: ['反向链仍属于一个弱连通分量。', '孤立点和仅有自环的点各形成一个分量。', '平行边不重复计数顶点。'],
    sampleExplanation: '边 2 到 0、2 到 1 虽不能让三点沿原方向互达，但忽略方向后它们连通；3、4 属于另一分量，5 独立。',
    questions: ['强连通分量是否一定包含于某个弱连通分量？为什么？', '不显式构造底层无向图，使用并查集应如何处理每条边？']
  },
  {
    id: 18, slug: 'unreachable-pairs', title: '无法互达的点对数', ...lc(2316, 'count-unreachable-pairs-of-nodes-in-an-undirected-graph'), article: 3,
    description: '按连通分量统计跨分量无序点对，避免二次枚举并处理 64 位答案。',
    statement: '给定简单无向图，统计不同顶点组成的无序点对 {u,v} 中，无法通过路径互达的点对数量。{u,v} 与 {v,u} 是同一对，顶点不能与自身配对。',
    input: '第一行 `n m`，随后 m 行无向边 `u v`。', output: '输出无法互达的无序点对数，使用 64 位整数。',
    constraints: '`1 <= n <= 100000`，`0 <= m <= 200000`，顶点编号 `0..n-1`；无自环重边。',
    steps: ['求出每个连通分量的大小 s。', '设此前分量共包含 seen 个顶点，累加 seen*s 后更新 seen，避免每对计算两次。'],
    proof: '同一分量内所有点对可达，不同分量之间所有点对不可达。处理新分量时，它与此前分量构成 seen*s 对，每个跨分量点对只在后出现的那个分量被处理时计入一次。',
    complexity: '时间 `O(n+m)`，空间 `O(n+m)`。不需要枚举 n(n-1)/2 个点对。',
    pitfall: '先以 int 相乘再赋给 long long 仍会溢出；无序点对不能重复累计两个方向。',
    boundaries: ['n=1：输出 0。', '全连通：输出 0。', 'n=100000、m=0：输出 4999950000。'],
    sampleExplanation: '分量大小为 4、2、1，跨分量点对数为 4*2+4*1+2*1=14。',
    questions: ['如何用总点对数减去各分量内部点对数得到同一结果？', '为什么使用 size*(n-size) 求和时最后需要除以 2？']
  },
  {
    id: 19, slug: 'complete-components', title: '完全连通分量计数', ...lc(2685, 'count-the-number-of-complete-components'), article: 3,
    description: '在分量内部比较实际边数与完全图边数，识别全部完全分量。',
    statement: '一个连通分量若其中任意两个不同顶点之间都有直接边，则称为完全连通分量。给定简单无向图，输出完全连通分量的数量。孤立顶点也算完全连通分量。',
    input: '第一行 `n m`，随后 m 行无向边 `u v`。', output: '输出完全连通分量数量。',
    constraints: '`1 <= n <= 50`，`0 <= m <= n(n-1)/2`，顶点编号 `0..n-1`；无自环重边。',
    steps: ['遍历每个连通分量，累计顶点数 s 和度数和 D。', '利用 D=2e，检查 e 是否达到 s(s-1)/2。'],
    proof: '简单图中 s 个顶点之间最多有 s(s-1)/2 条边。实际边数达到此上界当且仅当每个不同点对都有边。连通分量不存在通往其他分量的边，所以在分量内累加度数正好得到其内部边数的两倍。',
    complexity: '时间 `O(n+m)`，空间 `O(n+m)`。',
    pitfall: '连通不等于完全；若允许平行边，单看边数达到上限不能推出完全。',
    boundaries: ['单点分量 s=1、e=0 满足公式。', '两个顶点一条边是完全分量。', '三点链连通但不完全。'],
    sampleExplanation: '三点三角形、两点一条边、单个孤立点分别构成一个完全分量，因此答案为 3。',
    questions: ['可以改为检查每个顶点的度均为 s-1 吗？', '“每个点的度都相同”是否就意味着该分量完全？请给反例。']
  },
  {
    id: 20, slug: 'building-roads', title: 'Building Roads', ...cses(1666), article: 3,
    description: '以最少新增道路连接所有城市，并构造确定的最优方案。',
    statement: '给定 n 座城市和已有双向道路，需要添加尽量少的道路，使任意两座城市都能互相到达。设现有分量各自的最小城市编号从小到大为 r1..rk，本 Lab 要求依次添加 r1-r2、r1-r3、...、r1-rk，输出该规范方案。',
    input: '第一行 `n m`，随后 m 行已有道路 `u v`。',
    output: '第一行最少新增道路数 `k-1`。随后 k-1 行按上述顺序输出道路两个端点。原图已连通时仅输出 `0`。',
    constraints: '本 Lab：`1 <= n <= 100000`，`0 <= m <= 200000`，城市编号 `1..n`；无自环、无重边。',
    steps: ['找出每个分量的最小城市编号，按编号升序排列。', '把其余分量的代表城市都连接到最小代表城市，输出固定顺序。'],
    proof: '一条新增道路最多使分量数减少 1，所以 k 个分量至少需要 k-1 条道路。以一个分量为中心连接其他所有分量恰好使用 k-1 条，且图变连通，因此达到下界。',
    complexity: '按城市编号递增启动遍历时代表天然有序，总时间 `O(n+m)`，空间 `O(n+m)`。',
    pitfall: '输出 k 条道路不是最优；原题任意最优方案可以合法，但本地必须满足题面规范顺序。',
    boundaries: ['只有一个城市：输出 0。', '空图：新增 n-1 条道路。', '顶点编号交错的分量：代表必须是每个分量的最小顶点。'],
    sampleExplanation: '三个分量是 {1,3}、{2,4}、{5,6}，代表为 1、2、5。新增 1-2 和 1-5 即可连通全部城市。',
    questions: ['为什么新增道路连接同一分量内部的两点不能帮助达到最少条数？', '将代表连接成一条链是否也最优？它是否符合本 Lab 的输出规则？']
  },
  {
    id: 21, slug: 'flight-routes-check', title: 'Flight Routes Check', ...cses(1682), article: 3, difficulty: '进阶',
    description: '通过原图和转置图的两次遍历判断强连通，并给出不可达见证。',
    statement: '城市之间的航线有方向。判断任意城市 a 是否都能沿航线到达任意城市 b。若不能，输出一个不可达的有序点对。为了让答案唯一，先检查从城市 1 无法到达的城市，若存在则取最小编号 v 并输出 1 v；否则取无法到达城市 1 的最小编号 v，输出 v 1。',
    input: '第一行 `n m`，随后 m 行有向航线 `u v`。',
    output: '强连通时输出一行 `YES`。否则第一行 `NO`，第二行输出按上述优先级选择的不可达有序点对 `a b`，表示 a 无法到达 b。',
    constraints: '本 Lab：`1 <= n <= 100000`，`0 <= m <= 200000`，城市编号 `1..n`；允许自环和平行边。',
    steps: ['从 1 遍历原图，按编号找第一个未访问顶点。', '若原图遍历全部可达，再从 1 遍历转置图，检查是否每点都能回到 1。'],
    proof: '若所有点都能从 1 到达且都能到达 1，则任意 a 到 b 可经过 a 到 1 到 b，故图强连通。反之，任一次遍历发现未访问点就直接给出对应方向的不可达见证。',
    complexity: '时间 `O(n+m)`，空间 `O(n+m)`，不需要从每个点分别遍历。',
    pitfall: '只有“1 能到达所有点”不足以证明强连通；反图中的不可达见证必须交换方向。',
    boundaries: ['单点空图：YES。', '从 1 出发的单向链：能向外到达但不能返回，NO。', '两种失败同时存在：优先输出正向失败的最小编号。'],
    sampleExplanation: '1 能沿链到达全部城市，但城市 2 无法回到 1，按规则输出 NO 和 2 1。',
    questions: ['两次遍历的固定起点可以换成其他顶点吗？证明还成立吗？', '为什么只求弱连通分量数无法解决本题？']
  },
  {
    id: 22, slug: 'planets-and-kingdoms', title: 'Planets and Kingdoms', ...cses(1683), article: 3, difficulty: '进阶',
    description: '用 Kosaraju 划分全部强连通分量，并消除遍历顺序对编号的影响。',
    statement: '星球间存在有向通道。若两个星球能够互相到达，它们属于同一个王国；每个王国就是一个强连通分量。求王国数量及每个星球所属编号。本 Lab 按每个王国最小星球编号的升序，将王国规范编号为 1..k；编号不一定是拓扑序。',
    input: '第一行 `n m`，随后 m 行有向通道 `u v`。',
    output: '第一行王国数 k；第二行 n 个整数，按星球 1..n 的顺序输出其规范王国编号。',
    constraints: '本 Lab：`1 <= n <= 100000`，`0 <= m <= 200000`，星球编号 `1..n`；允许自环和平行边。',
    steps: ['第一次完整 DFS 记录顶点的退出顺序，用显式栈帧保留邻居游标。', '按退出顺序逆序在转置图遍历，每次得到一个 SCC。', '按原顶点升序扫描，将原始 SCC 编号转换为最小顶点升序编号。'],
    proof: '将 SCC 缩成 DAG。若原图有跨分量边 C 到 D，则 C 的最大 DFS 退出时间大于 D 的最大退出时间。按退出时间递减处理转置图时，当前分量无法进入尚未处理的其他分量，因此每次遍历恰好取出一个 SCC。最终重编号只改变标签，不改变划分。',
    complexity: '时间 `O(n+m)`，空间 `O(n+m)`；最小顶点规范化通过一次顶点扫描完成，不必排序全部顶点。',
    pitfall: '入栈或发现顺序不是退出顺序；第一次 DFS 遇到已访问点必须跳过，第二次要使用反图。',
    boundaries: ['DAG：每点都是独立王国。', '一整个有向环：一个王国。', '多个 SCC 单向连接：不能合并成一个。'],
    sampleExplanation: '{1,3} 互达、{2,4} 互达、5 单独成组，虽有跨组单向通道，仍分成三个王国，标签是 1 2 1 2 3。',
    questions: ['为什么迭代 DFS 需要记录“下一条待探索的边”，而非只把顶点压栈？', '按最小顶点得到的规范编号与拓扑排序有什么区别？']
  },
  {
    id: 23, slug: 'scc-condensation', title: 'SCC 凝聚图构造', ...course(3), article: 3, difficulty: '进阶',
    description: '将每个强连通分量缩成一个点，构造无自环、无重边的有向无环图。',
    statement: '把有向图的每个 SCC 缩成一个新顶点。原图中连接不同 SCC 的边在新图中保留；同一 SCC 内部的边丢弃，重复的跨分量边只保留一次。新顶点按所属 SCC 最小原顶点的升序编号为 1..k，请输出映射与规范凝聚图。',
    input: '第一行 `n m`，随后 m 行有向边 `u v`。',
    output: '第一行 `k p`，表示 SCC 数和去重后的凝聚边数；第二行按原顶点 0..n-1 输出 n 个规范 SCC 标签；随后 p 行凝聚边 `a b`，按 a 升序、a 相同时按 b 升序排列。p=0 时不输出边行。',
    constraints: '`1 <= n <= 100000`，`0 <= m <= 200000`，原顶点编号 `0..n-1`，SCC 编号 `1..k`；输入允许自环和平行边，输出凝聚图禁止自环和重边。',
    steps: ['计算所有 SCC，并按最小原顶点重编号。', '遍历原边，丢弃端点标签相等的边，其余映射到分量编号对。', '排序编号对并去重，再输出 k、p、顶点映射与边集。'],
    proof: '每条跨 SCC 的原边准确产生一条分量间关系，去重不改变这种关系。若凝聚图存在有向环，环上各 SCC 将两两互达，应属于同一 SCC，与划分定义矛盾，因此凝聚图必为 DAG。',
    complexity: 'SCC 划分与规范化 `O(n+m)`，凝聚边排序去重 `O(m log(m+1))`，总空间 `O(n+m)`。',
    pitfall: '不能在原图上直接去重后就忽略 SCC 内部边；不同原边也可能映射为同一条凝聚边。',
    boundaries: ['自环和 SCC 内部普通边都要丢弃。', '不同原边连接同一对 SCC：输出一次。', '空图有 n 个 SCC，但 p=0。'],
    sampleExplanation: 'SCC 为 {0,2}、{1,3}、{4}，规范标签为 1 2 1 2 3。多条原边映射成同一条 1 到 2 的边，去重后只保留 1 2 和 2 3。',
    questions: ['凝聚图为什么一定无环？', '如果要求保留两分量间的边数而非去重，输出和数据结构应如何调整？']
  }
]

export function labName(item) { return `E-06-${String(item.id).padStart(2, '0')}-${item.slug}` }
export function labPath(item) { return `labs/chapter-06/exercise/${labName(item)}` }
