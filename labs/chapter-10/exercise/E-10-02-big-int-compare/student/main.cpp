#include <iostream>
#include <string>
using namespace std;

int main() {
    int n;
    cin >> n;

    // TODO: 读入 n 个超长整数（票数），找出最大的一张票对应的编号和票数。
    // 提示：票数可能远超内置整型范围，用 string 存储。
    // 比较两条规则：
    //   1) 位数长的更大；
    //   2) 位数相同 → 按字典序比较（即数值大小）。
    // 用一个 string 记录当前最大值，一个 int 记录它的编号。
    // 输出两行：第一行编号，第二行票数。

    return 0;
}
