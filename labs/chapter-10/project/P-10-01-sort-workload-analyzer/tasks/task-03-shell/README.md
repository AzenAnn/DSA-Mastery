# Lab 10-P-01：基础排序工作负载评测器 · 任务三

实现 `sortlab::ShellSorter` 的 `sort()`，支持两种增量序列：

- `GapSequence::Shell`：`n/2, n/4, …, 1`；
- `GapSequence::Hibbard`：`1, 3, 7, 15, …`（`2^k - 1`），取不超过 `n/3` 的最大项，再 `(h-1)/2` 递减到 1。

对每个增量 `h`，做一次按步长 `h` 的插入排序。计数口径与任务一一致。希尔排序不稳定（间隔交换会跨过相等 key）。
