#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: 根据 README 中的递归契约卡实现算法。
    // 先保留一个能编译、但不会通过完整测试的起点。
    string token;
    while (cin >> token) {
    }
    cout << 0 << '\n';
    return 0;
}
