#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: 根据 README 完成递归或分治算法。保留标准输入输出格式。
    string token;
    while (cin >> token) {
    }
    cout << 0 << '\n';
    return 0;
}
