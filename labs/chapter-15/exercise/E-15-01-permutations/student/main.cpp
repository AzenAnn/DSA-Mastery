#include <functional>
#include <iomanip>
#include <iostream>
#include <vector>

using namespace std;

void solve(int n) {
    (void)n;
    // TODO: enumerate every permutation in lexicographic order.

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    if (!(cin >> n)) return 0;
    solve(n);
    return 0;
}
