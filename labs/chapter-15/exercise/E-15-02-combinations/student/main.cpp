#include <functional>
#include <iomanip>
#include <iostream>
#include <vector>

using namespace std;

void solve(int n, int r) {
    (void)n;
    (void)r;
    // TODO: enumerate increasing combinations.

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, r;
    if (!(cin >> n >> r)) return 0;
    solve(n, r);
    return 0;
}
