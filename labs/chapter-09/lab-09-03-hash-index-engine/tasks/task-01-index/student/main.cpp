#include <iostream>

int main() {
    // TODO: 实现散列索引的命令行驱动。
    // 第一行：模式（chaining / linear / quadratic）与表长 m。
    // 指令：put k v / get k / erase k / contains k / probes k / size / loadfactor。
    // get 未命中输出 null；erase/contains 输出 1 或 0；probes 输出查找比较次数。
    return 0;
}
