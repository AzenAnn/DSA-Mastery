#include <iostream>

int main() {
    // TODO: 实现链地址法与线性探测两种散列表。
    // 第一行选择模式（chaining / probing），第二行为表长 m。
    // 支持指令：
    //   insert k  -> 插入非负整数 k（重复插入忽略）
    //   find k    -> 命中输出 1，未命中输出 0
    //   delete k  -> 删除成功输出 1，未找到输出 0（探测法用墓碑标记）
    //   probe k   -> 输出查找 k 的比较/探测次数
    //   dump      -> 输出整张表（详见 README）
    return 0;
}
