# 实验与选型报告

> 复制本模板并填写。正式计时至少 7 轮，报告中位数；区分比较次数、空间估算与墙钟时间。

## 1. 实验方法

- 操作系统 / CPU / 编译器版本 / 构建类型：
- 工作负载参数（profile、size、seed、操作规模）：
- 预热轮数 / 正式轮数 / 汇总方式：
- 计时是否包含构造、初始化、正确性校验与输出：

## 2. 数据

| Profile | 规模 | Seed | naive 比较次数 | kmp 比较次数 | nextval 比较次数 | 中位时间（ms） |
| --- | --- | --- | ---: | ---: | ---: | ---: |
| random-text | 256 / 4096 / 65536 | ... | | | | |
| repeated-prefix | ... | ... | | | | |
| worst-case | ... | ... | | | | |
| realistic | ... | ... | | | | |
| stream | ... | ... | | | | |

## 3. 原因分析

- 把观察到的差异连接到字符比较次数、主串回退与 next/nextval 收益条件；
- 说明同为 O(n+m) 时朴素为什么仍可能更快；
- 不把比较次数解释成 CPU 周期，也不根据一次计时宣布"谁更快"。

## 4. 工程选型与边界

至少两个场景（如：流式顺序输入、文本编辑器查找、固定文本多次查找），每个场景写明访问比例、定位输入、规模，并说明哪些条件变化会使结论反转。

## 5. 复盘

记录一个被测试捕获的错误、不变量破坏方式与最小回归用例。
