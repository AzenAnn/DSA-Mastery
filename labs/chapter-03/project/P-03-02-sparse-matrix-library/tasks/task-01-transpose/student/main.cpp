#include <iostream>
#include <vector>

int main() {
    // TODO: 用快速转置（num/cpot）把稀疏矩阵三元组转置并按 (row, col) 升序输出。
    // 输入：第一行 rows cols count，随后 count 行 "row col value"。
    // 输出：转置后的非零元，每行 "row col value"（按 (row, col) 升序）。
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int rows, cols, count;
    std::cin >> rows >> cols >> count;
    // 实现转置并输出
    return 0;
}
