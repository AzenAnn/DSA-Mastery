# 稀疏矩阵运算实验与选型报告

> 复制本模板并填写。正式计时至少 7 轮取中位数；区分非零元个数、空间估算与墙钟时间。

## 1. 实验方法

- 操作系统 / CPU / 编译器版本 / 构建类型：
- 矩阵规模与稀疏度（rows × cols、非零元个数 t、稀疏因子 δ = t/(mn)）：
- 数据生成方式（随机位置 / 带状 / 对角线集中）与 seed：
- 预热轮数 / 正式轮数 / 汇总方式：
- 计时是否包含构造、排序、正确性校验与输出：

## 2. 数据

| 场景 | 规模 | t | δ | 转置耗时(ms) | 加法耗时(ms) | 乘法耗时(ms) |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 稀疏 | 128×128 | ... | ... | | | |
| 中等 | 512×512 | ... | ... | | | |
| 稠密 | 1024×1024 | ... | ... | | | |

## 3. 原因分析

- 把观察到的差异连接到三元组顺序表的 `num`/`cpot` 一趟转置与普通转置的 `O(cols × t)`；
- 说明加法、乘法在稀疏与稠密下的不同主导项；
- 不把非零元个数解释成 CPU 周期，也不根据一次计时宣布"谁更快"。

## 4. 工程选型与边界

至少两个场景（如：随机稀疏矩阵的批量运算、带状矩阵、需要频繁插入删除的矩阵相加），每个场景写明访问模式、定位输入、规模，并说明哪些条件变化（稀疏度接近稠密、非零元位置动态变化）会使结论反转，进而从三元组转向十字链表。

## 5. 复盘

记录一个被测试捕获的错误、不变量破坏方式与最小回归用例。
