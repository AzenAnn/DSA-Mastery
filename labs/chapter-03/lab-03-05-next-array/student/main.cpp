#include <iostream>
#include <string>

int main() {
    // TODO: 读入模式串，输出两行：next 数组、nextval 数组。
    // 约定：0-based，next[0] = nextval[0] = -1，元素用空格分隔。
    std::string p;
    std::getline(std::cin, p);
    std::cout << -1 << '\n';
    std::cout << -1 << '\n';
    return 0;
}
