#include <iostream>
#include <queue>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    // TODO: 读取各段长度，用最小堆求最优合并总代价
    long long total = 0;
    cout << total << '\n';
    return 0;
}
