#include <iostream>
#include <queue>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    // TODO: 读取频率，用最小堆构建哈夫曼树，计算编码总长度
    long long total = 0;
    cout << total << '\n';
    return 0;
}
