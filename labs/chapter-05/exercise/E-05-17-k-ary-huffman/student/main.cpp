#include <iostream>
#include <queue>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, k;
    cin >> n >> k;
    // TODO: 读取权值，构建 k 叉哈夫曼树，计算最小总代价
    long long total = 0;
    cout << total << '\n';
    return 0;
}
