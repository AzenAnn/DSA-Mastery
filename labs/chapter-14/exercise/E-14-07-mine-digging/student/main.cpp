#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: 按 README 的状态卡完成动态规划。先读完输入，保留一个可编译但不会满分的起点。
    string token;
    while (cin >> token) {
    }
    cout << 1 << '\n' << 0 << '\n';
    return 0;
}
