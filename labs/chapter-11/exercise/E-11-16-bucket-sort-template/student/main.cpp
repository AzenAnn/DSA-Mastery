#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;

    // TODO: 按值域分桶，用桶排序从小到大输出一行。
    // 提示：
    //   1. 值域 0..10^6，开一个长度 1000001 的计数桶数组（用 vector<int> 并全部置 0）；
    //   2. 读入 n 个数，对每个数 x 执行 cnt[x]++（把数丢进下标为 x 的桶）；
    //   3. 从小到大遍历整个值域，把每个值 i 连续输出 cnt[i] 次（空格分隔），即升序结果。

    return 0;
}
