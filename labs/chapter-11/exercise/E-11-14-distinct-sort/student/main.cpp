#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    // TODO: 用计数数组实现「去重 + 排序」。
    // 提示：
    //   1. 值域只有 1..1000，开一个长度 1001 的计数数组（下标 1..1000），全部置 0；
    //   2. 读入 n 个数，把每个数 x 对应的 cnt[x] 置 1（同一数值只记一次，即去重）；
    //   3. 统计 cnt 中值为 1 的位置个数，得到去重后数量 M，先输出 M；
    //   4. 再按 1..1000 的顺序，把 cnt[i] 为 1 的 i 依次输出（空格分隔），即升序结果。

    return 0;
}
