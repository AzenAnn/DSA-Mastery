#include <iostream>
#include <queue>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;

    // TODO: 用对顶堆求每个奇数长度前缀的中位数。
    // 提示：
    //   big（大根堆）保存较小的一半，small（小根堆）保存较大的一半；
    //   每次插入后保持 big.size() == small.size() 或 big.size() == small.size()+1，
    //   当 i 为奇数时，中位数就是 big.top()。

    return 0;
}
