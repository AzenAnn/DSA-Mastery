#include <iostream>
#include <queue>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;

    // TODO: 用小根堆完成 n 次操作。
    // 提示：priority_queue<long long, vector<long long>, greater<long long>> pq;
    //   op == 1：读入 x，入堆；
    //   op == 2：输出堆顶（当前最小元素）并删除。

    return 0;
}
