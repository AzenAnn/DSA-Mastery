#include <iostream>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;

    // TODO: 读入 n 张选票（每张票是一个 1..m 的编号），
    // 用计数排序按编号从小到大输出所有票。
    // 提示：值域很小且已知（1..m），开一个 int cnt[m+1]（或 vector<int> cnt(m+1)），
    // 用「编号本身」当下标统计每个编号出现几次，
    // 再按编号 1..m 依次输出 cnt[i] 次。
    // 注意：m 最大可达 999，数组开够大小。

    return 0;
}
