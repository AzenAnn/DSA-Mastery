# Lab 08-P-01：自平衡查找树——AVL 旋转维护与退化对比

实现 `contracts/avl.hpp` 中的 `avl::AvlTree`。每次插入、删除后都必须保持：

- 中序遍历严格升序；
- 任意结点平衡因子在 {-1, 0, 1} 内；
- 结点高度等于左右子树较大高度加 1。

插入失衡用 LL / RR / LR / RL 四种旋转修复；删除可能连续多处失衡，需逐层回溯重新平衡。
