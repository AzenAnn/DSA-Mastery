#include "../support/tree.hpp"

std::vector<GeneralNode*> binaryToForest(const BinaryNode*, std::vector<GeneralNode>&) {
    // TODO: decode root and child sibling chains into ordered arrays.
    return {};
}
