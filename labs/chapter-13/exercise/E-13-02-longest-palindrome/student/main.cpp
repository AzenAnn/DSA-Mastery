#include <iostream>
#include <string>

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    std::string s;
    if (!(std::cin >> s)) return 0;

    // 学生需要在这里完成最长回文串长度的计算。
    std::cout << 0 << '\n';
    return 0;
}
