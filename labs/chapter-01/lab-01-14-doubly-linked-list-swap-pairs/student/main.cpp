#include <iostream>

struct Node {
    long long value{};
    Node* prev = nullptr;
    Node* next = nullptr;
};

int main() {
    // TODO: 读入双链表，交换每对相邻节点后输出。
    // 要求：修改 prev/next 链接完成交换，不允许只交换值。
    return 0;
}
