#include <iostream>

struct Node {
    long long value{};
    Node* prev = nullptr;
    Node* next = nullptr;
};

int main() {
    // TODO: 读入双链表，判断是否为回文序列。
    // 要求：利用双向指针从两端向中间遍历，O(n) 时间，O(1) 额外空间。
    return 0;
}
