# Task 1：动态顺序表

在 `student/sequential_list.cpp` 中完成 `contracts/sequential_list.hpp` 的全部函数。不要修改公共头文件。

关键验收：List 行为与越界契约正确；容量从 8 开始、满载倍增、四分之一满才减半；搬移和缓冲区更换计数符合顶层 README；clear 后可复用且不变量保持。

```powershell
make run TASK=sequential-list
```
