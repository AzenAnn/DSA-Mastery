#include <iostream>
#include <vector>

struct Slot {
    long long data{};
    int next = -1;
};

int main() {
    // TODO: 读入静态链表的节点池和头节点游标，逆置后输出节点值序列。
    // 要求：只修改 next 游标，不交换 data。
    return 0;
}
